import { NextPage } from 'next';

import Layout from '@/components/Layout';

interface Props {}

const Home: NextPage<Props> = ({}) => {
  return <div>
    <Layout>
      Home
    </Layout>
  </div>
}

export default Home